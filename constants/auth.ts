import { pathNames } from "./pathname.const";

export const publicPaths = [pathNames.login, pathNames.register];

export const AUTH_COOKIE_NAME = "token";
export const AUTH_COOKIE_EXPIRES = 7; // days
